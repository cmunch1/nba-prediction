
name: test



jobs:
  scrape_features:
    #runs-on: windows-latest
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2

      - name: setup python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9.13'
          
      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          #pip install -r requirements.txt
          #python -m pip install jupyter selenium bs4 webdriver_manager
          #python -m pip install jupyter pyppeteer bs4 
          python -m pip install jupyter scrapingant-client bs4 scrapingant-client[async]
        
        
      
      #- name: Setup Chrome
      #  uses: browser-actions/setup-chrome@latest
        
      #chromium setup
      #- name: Installed package list
      #  run: apt list --installed
      #- name: Remove Chrome
      #  run: sudo apt purge google-chrome-stable
      #- name: Remove default Chromium
      #  run: sudo apt purge chromium-browser
      #- name: Install a new Chromium
      #  run: sudo apt install -y chromium-browser
        

      - name: run test script
        env: 
          HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
          NEPTUNE_API_TOKEN: ${{ secrets.NEPTUNE_API_TOKEN }}
          SCRAPINGANT_API_KEY: ${{ secrets.SCRAPINGANT_API_KEY }}
          
        run: 
          python ./src/test3.py

      - name: save results
        uses: actions/upload-artifact@v3
        with:
          name: nba_boxscores
          path: nba_boxscores.html

